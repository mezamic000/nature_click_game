import React from "react";
import './style.css'

const Instructions = () => {
  return (
    <section>
      <h2 className="howTo-Start">Click on any image to start!</h2>
      <h4 className="rules">Earn a point for every new image you select. Do not select the same image more than once. If you do, your score will reset to zero. Good Luck!</h4>
    </section>
  )
}

export default Instructions;